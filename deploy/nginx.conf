upstream fastapi_app {
    server 127.0.0.1:8000;
    keepalive 128;
}

# ─── HTTP → HTTPS Redirect ─────────────────────────────────────────────────────
server {
    listen 80;
    server_name DOMAIN_PLACEHOLDER;

    # Let's Encrypt / Certbot ACME challenge path
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # All other traffic → redirect to HTTPS
    location / {
        return 301 https://$host$request_uri;
    }
}

# ─── HTTPS (Main) ──────────────────────────────────────────────────────────────
server {
    listen 443 ssl http2;
    server_name DOMAIN_PLACEHOLDER;

    # ── SSL Certificate (Certbot / Let's Encrypt) ─────────────────────────
    # Setup: sudo certbot --nginx -d DOMAIN_PLACEHOLDER
    ssl_certificate     /etc/letsencrypt/live/DOMAIN_PLACEHOLDER/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/DOMAIN_PLACEHOLDER/privkey.pem;

    # ── SSL Hardening ─────────────────────────────────────────────────────
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5:!RC4;
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    # ── Security Headers ──────────────────────────────────────────────────
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;
    add_header Content-Security-Policy "frame-ancestors 'self'; base-uri 'self'; object-src 'none'" always;

    # ── Static Files (Cached) ─────────────────────────────────────────────
    # Only expose browser-safe static directories. Client portal index.html is
    # served by FastAPI after auth, and server bundles must never be public.
    location /static/css/ {
        alias /var/www/buykori-adsync/app/static/css/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    location /static/client-portal/assets/ {
        alias /var/www/buykori-adsync/app/static/client-portal/assets/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    location /static/ {
        return 404;
    }

    # ── Proxy to FastAPI ──────────────────────────────────────────────────
    location / {
        proxy_pass http://fastapi_app;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_buffering on;
        proxy_buffers 16 16k;
        proxy_buffer_size 32k;
        proxy_connect_timeout 5s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
    }

    # General upload ceiling. Public tracking/webhook routes are also guarded
    # inside FastAPI with tighter env-configurable Content-Length checks.
    client_max_body_size 10M;

    # Gzip Compression
    gzip on;
    gzip_comp_level 4;
    gzip_min_length 256;
    gzip_vary on;
    gzip_proxied any;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
}
